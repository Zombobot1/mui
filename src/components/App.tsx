import { TextField, Typography } from '@material-ui/core';

export const App = () => {
  return (
    <>
      <Typography sx={{ fontSize: '3rem' }}>Not nunito!</Typography>
      <TextField placeholder='Nunito placeholder' />
    </>
  );
};
